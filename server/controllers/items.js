var mongoose = require('mongoose');
//Require the necessary Schema/Models
// var Item = mongoose.model('Item'); <- for example

module.exports = {
	/*Add the actual route logic here. Remember to respond with something, even if it's just 'true'
		ex:
		find: (req, res)=>{
	
		},
		add: (req, res)=>{
	
		}
	*/
	// getItems: (res, req)=>{
		
	// }
}